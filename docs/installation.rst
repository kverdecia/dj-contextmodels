============
Installation
============

At the command line::

    $ easy_install dj-contextmodels

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-contextmodels
    $ pip install dj-contextmodels
